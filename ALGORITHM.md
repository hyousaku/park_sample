# アルゴリズム解説

このドキュメントでは `main.py`（グリッドモード）と `bubble_mode.py`（バブルモード）のアルゴリズムを説明します。

---

## 共通の処理フロー

両モードとも、以下のループを **1秒間に30回** くり返します。

```
カメラから映像を1枚取得
　↓
AIで人の頭の位置を検出
　↓
当たり判定（頭がマス or 円に入っているか）
　↓
音を鳴らす・画面を更新
```

---

## 1. 人の頭を検出するアルゴリズム（共通）

**使用AI：YOLOv8-pose**

YOLOv8-poseは画像の中の人の「骨格ポイント（キーポイント）」を17箇所推定します。
このプログラムでは **0番目のキーポイント＝鼻** を頭の中心として使います。

```
カメラ映像（1フレーム）
　↓
YOLOv8-poseで推論
　↓
各人の骨格キーポイントを取得
　↓
鼻（キーポイント[0]）の座標(x, y)を取り出す
　↓
鼻を中心に 80×80ピクセルの四角形を頭の領域として定義
　↓
頭の位置リスト [(x1,y1,x2,y2), ...] を返す
```

---

## 2. グリッドモード（main.py）

### 画面の構造

画面を **横16 × 縦9 = 144マス** に分割します。
各マスには **音の高さ（周波数）** と **色** がランダムに割り当てられています。

```
┌──┬──┬──┬──┬──┬──┬──┬──┬──┬──┬──┬──┬──┬──┬──┬──┐
│  │  │  │  │  │  │  │  │  │  │  │  │  │  │  │  │  ← 9行
├──┼──┼──┼──┼── ...                               │
│  │  │  │  │                                     │
└──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┘
            16列
```

### 当たり判定アルゴリズム

```
全144マスについてくり返す：
　↓
　頭の中心座標(cx, cy)がこのマスの四角形の中に入っているか？
　　YES → cell.active = True
　　　　　まだ音が鳴っていなければ → 音を鳴らす・cell_triggered = True
　　NO  → cell.active = False
　　　　　cell_triggered = False（次に入ったとき再び音が鳴るようリセット）
　　　　　マスの色をゆっくり薄くする（alpha を 10 ずつ減らす）
```

**ポイント：** `cell_triggered` フラグにより、**マスに入った瞬間だけ**音が鳴り、
居続けても音がくり返し鳴らないようになっています。

### 音階のシャッフル

**12秒ごと**に全マスの音の高さと色をランダムに再割り当てします。

```
time.time() で現在時刻を取得
　↓
最後にシャッフルした時刻から12秒経過した？
　YES → assign_sounds_to_grid() を呼び出して全マスをシャッフル
　NO  → 何もしない
```

---

## 3. バブルモード（bubble_mode.py）

### 円の動きアルゴリズム

画面上に **常時5個の円** がランダムな方向に移動します。

```
各円の初期化：
　- ランダムな位置(x, y)
　- ランダムな半径（80〜120px）
　- ランダムな角度と速度から速度ベクトル(vx, vy)を計算
　  vx = cos(angle) × speed
　  vy = sin(angle) × speed

毎フレームの更新：
　x += vx
　y += vy
　画面の端に当たったら？ → 速度の向きを反転（壁で跳ね返る）
```

### 当たり判定アルゴリズム

グリッドモードの「四角形の中に入っているか」と異なり、バブルモードでは **円との距離**で判定します。

```
各円について：
　↓
　頭の中心と円の中心の距離を計算
　  distance = √((円x - 頭x)² + (円y - 頭y)²)
　↓
　distance ≤ 円の半径 + 当たり判定半径(100px) ?
　　YES → 円を消す（alive = False）・音を鳴らす
　　NO  → 何もしない
```

### 円の管理アルゴリズム

```
毎フレーム：
　alive = False になった円を削除
　↓
　円の数が5個未満になった？
　　YES → 新しい円を追加して5個に補充
```

---

## 4. 音の生成アルゴリズム

コンピュータで音を作るには、**波（サイン波）** を数学で計算します。

### 基本の仕組み

```
音 = サイン波（基音）
    + 0.5 × サイン波（2倍の周波数）   ← 2倍音
    + 0.25 × サイン波（3倍の周波数）  ← 3倍音
```

複数の波を重ね合わせることで、シンプルな「ピー」という音ではなく、ピアノらしい音色になります。

### 減衰（だんだん小さくなる）

```
envelope = e^(-3t / duration)
```

時間 t が経つほど envelope が小さくなるため、
**最初は大きく、すぐ小さくなる**ピアノらしい音になります。

### グリッドモード vs バブルモードの違い

| | グリッドモード | バブルモード |
|---|---|---|
| **音の種類** | ピアノ音（倍音あり・緩やかな減衰） | 打楽器音（ノイズ混じり・急激な減衰） |
| **音階** | Dペンタトニック（8音） | 低め固定周波数（200〜550Hz） |
| **音が鳴るタイミング** | マスに入った瞬間 | 円に当たった瞬間（円は消える） |

---

## 5. 映像の左右反転

カメラ映像はそのままだと**鏡と逆向き**になるため、表示時に左右反転しています。

- **グリッドモード：** セルを描画するときにX座標を反転  
  `x = SCREEN_WIDTH - cell.rect.x - cell.rect.width`
- **バブルモード：** `pygame.transform.flip()` で映像ごと反転
