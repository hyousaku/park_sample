# バブルモード アルゴリズム解説

`bubble_mode.py` のアルゴリズムを詳しく説明します。

---

## 全体の処理フロー

**1秒間に30回**、以下の流れをくり返します。

```
カメラから映像を1枚取得
　↓
AIで人の頭の位置を検出（鼻の座標）
　↓
頭と各円の当たり判定
　↓
当たった円を消して音を鳴らす
　↓
消えた円を取り除き、不足分を補充
　↓
全円の位置を更新（移動・壁の跳ね返り）
　↓
映像（左右反転）＋円を画面に描画
```

---

## 1. 頭の検出アルゴリズム

**使用AI：YOLOv8-pose**

YOLOv8-poseは画像から人の骨格を17箇所のポイントで推定します。
このプログラムでは **0番目のキーポイント＝鼻** を頭の中心として使います。

```
カメラ映像（1フレーム）
　↓
YOLOv8-poseで推論
　↓
骨格キーポイントを取得
　↓
各人の鼻の座標(x, y)を取り出す
　↓
座標が(0, 0)でなければ有効な頭として記録
　↓
頭の位置リスト [(x, y), (x, y), ...] を返す
```

> グリッドモードと異なり、バブルモードでは頭の **中心点(x, y)のみ** を使います（四角形は不要）。

---

## 2. 円の移動アルゴリズム

### 初期化（円を生成するとき）

ランダムな方向・速さを **角度と速さ** から計算します。

```
angle = ランダムな角度（0°〜360°）
speed = ランダムな速さ（0.5〜4.0 px/フレーム）

vx（横の速さ） = cos(angle) × speed
vy（たての速さ） = sin(angle) × speed
```

たとえば angle=0° なら右方向、angle=90° なら下方向に動きます。

### 毎フレームの移動

```
x = x + vx   （横に vx ピクセル動く）
y = y + vy   （たてに vy ピクセル動く）
```

### 壁の跳ね返り

```
左端または右端にぶつかった？
　YES → vx を -vx に反転（横向きをひっくり返す）

上端または下端にぶつかった？
　YES → vy を -vy に反転（たて向きをひっくり返す）
```

これでボールが壁で跳ね返るような動きを再現しています。

---

## 3. 当たり判定アルゴリズム（距離で判定）

グリッドモードの「マスの中に入っているか」と異なり、
バブルモードでは **2点間の距離** で判定します。

```
距離 = √( (円の中心x - 頭x)² + (円の中心y - 頭y)² )
```

これは **三平方の定理（ピタゴラスの定理）** です。

```
距離 ≤ 円の半径 + 頭の当たり判定半径(100px) ?
　YES → 当たり！　→　円を消す・音を鳴らす
　NO  → 当たっていない
```

### 図解

```
       頭の中心
          ●
          |← 100px (HEAD_HIT_RADIUS)
          |
      ----+----  ← この範囲内に円の中心が入ったら当たり
          |
  ●───────┘
 円の中心  ↑
         円の半径(80〜120px)
```

---

## 4. 円の補充アルゴリズム

```
当たって消えた円を alive=False にする
　↓
alive=False の円をリストから取り除く
　↓
円の数 < 5 になった？
　YES → 新しい FloatingCircle を作ってリストに追加
　　　　（5個になるまでくり返す）
```

これにより画面上には **常に5個の円** が表示されます。

---

## 5. 打楽器音の生成アルゴリズム

ピアノ音（`main.py`）との違いは **ノイズの混入** と **速い減衰** です。

### ノイズを混ぜる

```
基本の波 = sin(2π × 周波数 × 時間)
ノイズ   = -0.3〜+0.3 のランダムな数
音       = 基本の波 × 0.7 + ノイズ × 0.3
```

ノイズを混ぜることで「シャッ」「パシッ」というドラムっぽい質感になります。

### 速い減衰（ピアノとの比較）

| | ピアノ音（main.py） | 打楽器音（bubble_mode.py） |
|---|---|---|
| **減衰の式** | `exp(-3 × t / duration)` | `exp(-8 × t / duration)` |
| **音の長さ** | 0.3秒 | 0.15秒 |
| **聞こえ方** | ゆっくり消える | パッと鳴ってすぐ消える |

---

## 6. 映像の左右反転

カメラ映像はそのままだと **鏡と逆向き**（自分が右に動くと映像では左に動く）になります。
バブルモードでは映像全体を左右反転して、自然な鏡のように見えるようにしています。

```python
frame_surface = pygame.transform.flip(frame_surface, True, False)
#                                                     ↑     ↑
#                                                   左右  上下
#                                                   反転  反転なし
```

> グリッドモードとの違い：グリッドモードは映像は反転せず、セルの描画位置だけを反転しています。

---

## クラス構成まとめ

```
PercussionSound        打楽器音を作って鳴らす
　└─ generate_percussion_tone()  音データを数学で生成
　└─ play()                      指定した音を再生

FloatingCircle         ふわふわ動く円1個を表す
　└─ update()          位置を更新・壁で跳ね返る
　└─ draw()            画面に半透明の円を描く
　└─ check_collision() 頭との距離で当たり判定

BubbleModeApp          アプリ全体を管理する
　└─ detect_head()     AIで頭の位置を検出
　└─ check_collisions() 全円×全頭の当たり判定
　└─ manage_circles()  消えた円の削除と補充
　└─ run()             メインループ
　└─ cleanup()         終了処理
```
